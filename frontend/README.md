# Toy Password Manager (frontend)

Quick start:

1. Install dependencies:
```bash
cd frontend
npm install
```

2. Run dev server:
```bash
npm run dev
```

Open `http://localhost:5173` in your browser. Ensure the backend is running at `http://localhost:8000`.
